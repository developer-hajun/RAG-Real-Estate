<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ssafy.realty.Mapper.SearchHistoryMapper">
    <select id="searchHistory" resultType="ssafy.realty.Entity.SearchHistory">
        select * from SearchHistory where user_id=#{userId}
    </select>

    <select id="searchHistoryTop3" resultType="ssafy.realty.Entity.SearchHistory">
        select * from SearchHistory where user_id=#{userId} order by createdDate desc limit 3
    </select>

    <insert id="addSearchHistory">
        INSERT INTO SearchHistory (text, createdDate, updatedDate, user_id)
        VALUES (
                   #{searchHistory.text},
                   #{searchHistory.createdDate},  #{searchHistory.updatedDate},
                   #{userId}
               );
    </insert>
</mapper>